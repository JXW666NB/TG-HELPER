【工具列表】

1. click(x=None, y=None, button='left', clicks=1) - 在指定坐标点击鼠标（不提供坐标则在当前位置点击）。
2. type_text(text, interval=0.0) - 模拟键盘输入文本。
3. move_mouse(x, y, duration=0.5) - 移动鼠标到指定坐标。

【注意事项】
- 这些操作默认会请求用户确认，可在设置中关闭。
- 坐标基于屏幕左上角原点，可使用 pyautogui 辅助定位。
- 模拟操作可能被某些应用拦截，请确保目标窗口处于活动状态。